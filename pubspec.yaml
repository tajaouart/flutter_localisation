name: flutter_localisation
description: A command-line tool for generating localization files for different flavors.
version: 1.0.0
repository: https://github.com/tajaouart/flutter_localisation

environment:
  sdk: ^3.4.1 #   sdk: ^3.5.4

dependencies:
  logging: ^1.0.2

dev_dependencies:
  test: ^1.16.0

executables:
  flutter_localisation: bin/flutter_localisation.dart

flutter:
  generate: true